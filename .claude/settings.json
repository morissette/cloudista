{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); cmd=$(echo \"$input\" | jq -r '.tool_input.command'); if echo \"$cmd\" | grep -q 'git commit'; then ruff check api/ || { echo '{\"decision\":\"block\",\"reason\":\"ruff lint failed — fix errors before committing\"}'; exit 0; }; fi",
            "timeout": 30,
            "statusMessage": "Running ruff lint check..."
          }
        ]
      }
    ]
  }
}
